class Solution:
    def restoreString(self, s: str, indices: List[int]) -> str:
        answer = [""] * len(s)

        for i in range (len(s)):
            letter = s[i]
            index = indices[i]

            answer[index] = letter
        return "". join(answer)


        






































































































































































































































































































        